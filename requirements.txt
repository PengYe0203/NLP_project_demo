python>=3.8

# AI Model APIs (choose at least one)
openai>=1.0.0          # For OpenAI API (GPT-3.5, GPT-4, etc.)
anthropic>=0.7.0       # For Claude API
requests>=2.31.0       # For local models (Ollama, etc.)

# Jupyter support
jupyter>=1.0.0
ipywidgets>=8.0.0      # For interactive widgets in notebook

# Utilities
python-dotenv>=1.0.0   # For managing API keys
